{
  "name": "rank-rush",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "An 8-wide endless chess expedition. Start with a king and a knight. Loot an army. Outrun the wake.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.8"
  }
}
