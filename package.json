{
  "name": "rank-rush",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "An 8-wide endless chess expedition. Start with a king and a knight. Loot an army. Outrun the wake.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "build:single": "npm run build && node scripts/bundle-single.mjs",
    "soak": "vite-node scripts/soak.ts",
    "fuzz": "vite-node scripts/fuzz.ts",
    "stress": "vite-node scripts/fuzz.ts 400 expedition && vite-node scripts/fuzz.ts 400 sprint && vite-node scripts/endgame.ts && vite-node scripts/determinism.ts && vite-node scripts/perf.ts"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.8"
  }
}
